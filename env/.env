# ======================================================================
#  AWS Multi-Account Security and Governance Baseline
#  Environment Configuration Template (.env.example)
#  Copy this file, rename it to ".env", and edit the values below.
# ======================================================================

# ----------------------------------------------------------------------
# ACCOUNT AND ALIAS SETTINGS
# ----------------------------------------------------------------------

# The alias you want to assign to this specific AWS account.
# This value must be unique across all AWS accounts you manage.
# Example: main-account-root, sandbox-dev, billing-central, etc.
ACCOUNT_ALIAS="primary-account"

# Reference aliases (used only for documentation or optional validation)
PRIMARY_ALIAS="primary-account"
SANDBOX_ALIAS="sandbox-account"

# ----------------------------------------------------------------------
# IAM USERS TO CREATE
# ----------------------------------------------------------------------

# Full administrative IAM user
ADMIN_USER="AdminUser"

# Read-only IAM user for audit, experimentation, or education
READONLY_USER="ReadOnlyUser"

# ----------------------------------------------------------------------
# TEMPORARY CONSOLE PASSWORDS
# ----------------------------------------------------------------------

# These passwords are only used for first-time console login.
# AWS will force you to change them after the first sign-in.
TEMP_ADMIN_PASSWORD="ChangeMe#2025!"
TEMP_RO_PASSWORD="ChangeMe#2025!"

# ----------------------------------------------------------------------
# DEFAULT REGION
# ----------------------------------------------------------------------

# Default region for informational commands (not enforced globally)
AWS_DEFAULT_REGION="us-east-1"

# ======================================================================
#  INSTRUCTIONS
# ======================================================================
# 1. Copy this file to env/.env
#      cp env/.env.example env/.env
#
# 2. Open env/.env and replace all placeholder values with real data:
#      - ACCOUNT_ALIAS must NOT remain "primary-account" or "sandbox-account"
#      - ADMIN_USER, READONLY_USER, and passwords must be changed
#
# 3. Save and close the file.
#
# 4. Then run the setup script:
#      bash scripts/aws-cli-setup-commands.sh
#
# 5. The script will refuse to run if any placeholder values remain.
# ======================================================================
